# Guards

The guards are a set of helpers that could be used to validate parameters and values before launching an exception. All 
Guards are located in the namespace `IsuCorp.Guards`, implements the **Guard** pattern, which basically is a class that 
contains a set of static methods that could be used to validate parameters and values, before the logic of the method 
is executed.